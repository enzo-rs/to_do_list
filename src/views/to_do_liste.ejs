<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= result[0].name  %></h1>

    <ul>
        
        <% result.forEach((el) => { %>
            <li class="list-item" ><a href="<%= `/task/${el.id_tasks}` %>"><%= el.description %></a></li>
        <% }) %>
    </ul>
</body>
</html>